<%= form_for(final) do |f| %>
  <% if final.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(final.errors.count, "error") %> prohibited this final from being saved:</h2>

      <ul>
      <% final.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name , class:"form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :quanity %>
    <%= f.number_field :quanity , class:"form-control" %>
  </div>

  <div class="actions">
    <%= f.submit :create, class:"btn btn-default"%>
  </div>
<% end %>
